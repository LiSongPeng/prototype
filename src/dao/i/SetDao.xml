<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.i.SetDao">
    <update id="updateSet" parameterType="entity.Set">
        update syssetting_form
        set annual_examination_time=#{annual_examination_teim},
            state1=#{state1},
            renewal_time=#{renewal_time},
            state2=#{state2},
            maintenance_mileage=#{maintenance_mileage},
            state3=#{state3},
            maintenance_time=#{maintenance_time},
            state4=#{state4},
            refresh_frequency=#{refresh_frequency}
        where id=#{id}
    </update>
    <select id="selectById" parameterType="int" resultType="entity.Set">
        select * from syssetting_form where id=#{id}
    </select>
</mapper>