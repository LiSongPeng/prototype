<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="main.java.dao.i.SetDao">
    <update id="updateSet" parameterType="main.java.entity.systemSetting">
        update syssetting_form
        systemSetting
            annual_examination_time=#{annual_examination_time},
            state1=#{state1},
            renewal_time=#{renewal_time},
            state2=#{state2},
            maintenance_mileage=#{maintenance_mileage},
            state3=#{state3},
            maintenance_time=#{maintenance_time},
            state4=#{state4},
            refresh_frequency=#{refresh_frequency},
            state5=#{state5}
        where id=#{id}
    </update>
    <select id="selectById" parameterType="int" resultType="main.java.entity.systemSetting">
        select * from syssetting_form where id=#{id}
    </select>
</mapper>